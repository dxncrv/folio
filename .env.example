# === PocketBase ===
# Public URL (browser-facing, used in client-side code)
PUBLIC_POCKETBASE_URL=http://127.0.0.1:8090

# Server-side URL (internal networking / SSR)
# POCKETBASE_INTERNAL_URL=http://pb.railway.internal:8080
POCKETBASE_URL=http://127.0.0.1:8090

# Production PB URL (used by pb:pull / pb:push scripts)
POCKETBASE_PROD_URL=https://your-pb-instance.example.com

# Admin credentials (shared between local + prod, or override with PB_LOCAL_ADMIN_*)
PB_ADMIN_EMAIL=admin@example.com
PB_ADMIN_PASSWORD=your-admin-password

# Optional: separate local admin credentials
# PB_LOCAL_ADMIN_EMAIL=local@example.com
# PB_LOCAL_ADMIN_PASSWORD=local-password

# === App ===
ADMIN_TOKEN=your-admin-token
# ADMIN_IP=127.0.0.1
